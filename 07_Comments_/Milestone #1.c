#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This function demonstrates basic arithmetic operations
 * on two numbers and prints the results.
 *
 * Return: Always returns 0 to indicate successful execution.
 */

int main(void)
{
	int num1 = 5, num2 = 2;

	printf("the First number is %d and the Second number is %d\n", 5, 2);
	printf("the addition of the 2 numbers is %d\n", 5 + 2);
	printf("the subtraction of the 2 numbers is %d\n", 5 - 2);
	printf("the multiplication of the 2 numbers is %d\n", 5 * 2);
	printf("the Division of the 2 numbers is %d\n", 5 / 2);
	printf("the remainder of the 2 numbers is %d\n", 5 % 2);
	return (0);
}